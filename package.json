{
  "dependencies": {
    "discord.js": "^14.24.1"
  },
  "devDependencies": {
    "eslint": "^9.38.0"
  },
  "scripts": {
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "dev": "bun run index.ts",
    "prod": "pm2 start --interpreter bun index.ts --name instagram-embed-bot"
  }
}
